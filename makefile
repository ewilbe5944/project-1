# This command will compile the project-1 program
compile:
	g++ project-1.cpp -o p1.exe
# This command will run the project-1 program
run:
	./p1.exe
# This command will clean out the directory of any output files
clean:
	rm *.exe
